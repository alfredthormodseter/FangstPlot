services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: fangst
      POSTGRES_PASSWORD: fangst
      POSTGRES_DB: fangstplot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: